port: 4517
secret: abcdefghi
prefix:
  primary: pris
  alt: [priscilla $mention cilla]
adapters:
  hipchat:
    exec: /vagrant/dev/golang/bin/priscilla-hipchat
    params:
      user: "priscilla@priscilla.chat"
      pass: "abcdefg"
      nick: "Priscilla"
responders:
    # active:
    #   echo:
    #     exec: /vagrant/dev/golang/bin/pris-echo
    #     secret_flag: "-screct"
    #     args: ["arg1", "arg2", "arg3"]
    passive:
    - match: ^ping$
      noprefix: false
      cmd: /bin/echo
      args: ["I am alive, are you?"]
    - match: ^.*$
      noprefix: false
      cmd: /bin/echo
      args: ["why?"]
      # "md5.*":
      #   prefix: true
      #   trim: true
      #   cmd: /usr/bin/md5sum
      #   args: ["arg1", "arg2", "arg3"]
      #   payload: true
      #   fallthrough: true
      #   unhandled: true
